{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://www.radengine.dev/schema/qir.json",
  "title": "Rad Query IR",
  "description": "The relation-graph query IR on the wire: a map of named relation nodes plus a root selector. Relations are values — operators consume relations and produce relations — and relationships are ordinary correlated relations materialised into output shapes by cardinality crossings. The normative semantics live in docs/design/qir-v2.md.",
  "$defs": {
    "Value": {
      "title": "Value",
      "description": "A single scalar value: a string, a number, a boolean, or null. Numbers are decoded with full 64-bit integer precision, and the binder types every literal by the column it meets — never by guessing."
    },
    "Query": {
      "title": "Query",
      "description": "A query graph: relation nodes keyed by caller-chosen names, and a root selector. Node references are plain strings; the graph must be acyclic and each node feeds exactly one consumer.",
      "type": "object",
      "required": ["nodes", "root"],
      "properties": {
        "nodes": {
          "description": "The relation nodes of the graph, keyed by name.",
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/Node" }
        },
        "root": { "$ref": "#/$defs/Root" }
      }
    },
    "Root": {
      "title": "Root",
      "description": "Selects the result node and how it materialises: `many` is an array of records, `first` is zero or one, `exactly_one` errors unless exactly one row exists, and `scalar` unwraps a single-column row to its value.",
      "type": "object",
      "required": ["node", "cardinality"],
      "properties": {
        "node": { "type": "string" },
        "cardinality": {
          "type": "string",
          "enum": ["many", "first", "exactly_one", "scalar"]
        }
      }
    },
    "Node": {
      "title": "Node",
      "description": "One relation operator, a flat tagged union selected by `kind`. `scan` introduces a table and binds its `scope` label (required, unique per query); `filter` keeps rows whose `predicate` is TRUE under three-valued logic; `project` establishes a new row type from `spread` scopes and computed `fields`, optionally binding an output `scope`; `join` combines `left` and `right` on a boolean condition; `aggregate` folds its input per distinct `groups` (none means exactly one row), optionally binding a `scope`; `order` gives a logical ordering; `slice` pages — `limit` omitted is unlimited, an explicit 0 keeps no rows.",
      "type": "object",
      "required": ["kind"],
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["scan", "filter", "project", "join", "aggregate", "order", "slice"]
        },
        "table": { "type": "string" },
        "scope": { "type": "string" },
        "input": { "type": "string" },
        "left": { "type": "string" },
        "right": { "type": "string" },
        "join": {
          "type": "string",
          "enum": ["inner", "left"]
        },
        "on": { "$ref": "#/$defs/Expr" },
        "predicate": { "$ref": "#/$defs/Expr" },
        "spread": {
          "description": "Scopes whose columns the projection re-exposes, before its computed fields. Name collisions are rejected.",
          "type": "array",
          "items": { "type": "string" }
        },
        "fields": {
          "type": "array",
          "items": { "$ref": "#/$defs/Field" }
        },
        "groups": {
          "type": "array",
          "items": { "$ref": "#/$defs/GroupTerm" }
        },
        "aggs": {
          "type": "array",
          "items": { "$ref": "#/$defs/AggTerm" }
        },
        "terms": {
          "type": "array",
          "items": { "$ref": "#/$defs/OrderTerm" }
        },
        "offset": { "type": "integer" },
        "limit": { "type": "integer" }
      }
    },
    "Expr": {
      "title": "Expr",
      "description": "A scalar expression, a flat tagged union selected by `kind`. `lit` carries a raw value; `col` names a `scope`'s `column`; `unary` applies `op` (not, negate, is_null, is_not_null) to `expr`; `binary` applies `op` (eq ne lt lte gt gte, and or, add sub mul div) to `left` and `right` — and/or are binary, longer chains left-fold; `cast` converts `expr` `to` a scalar type. The cardinality crossings — `exists`, `first`, `scalar`, `array` — reference a relation `node` and are the only doors from relations into expressions: exists is a never-null boolean, first materialises the row as a nested object or null, scalar asserts a single-column at-most-one relation and yields its value, array materialises every row as a nested array. Comparisons with NULL are UNKNOWN; is_null is the only NULL match.",
      "type": "object",
      "required": ["kind"],
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["lit", "col", "unary", "binary", "call", "cast", "exists", "first", "scalar", "array"]
        },
        "value": { "$ref": "#/$defs/Value" },
        "scope": { "type": "string" },
        "column": { "type": "string" },
        "op": { "type": "string" },
        "expr": { "$ref": "#/$defs/Expr" },
        "left": { "$ref": "#/$defs/Expr" },
        "right": { "$ref": "#/$defs/Expr" },
        "fn": { "type": "string" },
        "args": {
          "type": "array",
          "items": { "$ref": "#/$defs/Expr" }
        },
        "to": { "type": "string" },
        "node": { "type": "string" }
      }
    },
    "Field": {
      "title": "Field",
      "description": "One projected attribute: a crossing expression renders as a nested object, array, scalar, or boolean under `as`.",
      "type": "object",
      "required": ["as", "expr"],
      "properties": {
        "as": { "type": "string" },
        "expr": { "$ref": "#/$defs/Expr" }
      }
    },
    "GroupTerm": {
      "title": "GroupTerm",
      "description": "One grouping attribute. `as` names the output; omitted, a bare column reference's name is used.",
      "type": "object",
      "required": ["expr"],
      "properties": {
        "as": { "type": "string" },
        "expr": { "$ref": "#/$defs/Expr" }
      }
    },
    "AggTerm": {
      "title": "AggTerm",
      "description": "One fold, named `as` in the output. `count` with no `arg` counts rows, with an `arg` counts non-null values; `sum` and `avg` need a numeric argument; `min` and `max` accept any scalar. Folds skip NULLs; over nothing, count is 0 and every other fold is null.",
      "type": "object",
      "required": ["fn", "as"],
      "properties": {
        "fn": {
          "type": "string",
          "enum": ["count", "sum", "avg", "min", "max"]
        },
        "arg": { "$ref": "#/$defs/Expr" },
        "as": { "type": "string" }
      }
    },
    "OrderTerm": {
      "title": "OrderTerm",
      "description": "One ordering term; NULLs sort first ascending, last descending. A known unique key of the output is appended as a deterministic tie-breaker.",
      "type": "object",
      "required": ["expr"],
      "properties": {
        "expr": { "$ref": "#/$defs/Expr" },
        "desc": { "type": "boolean" }
      }
    }
  }
}
