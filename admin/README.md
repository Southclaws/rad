# Administration UI

Rad comes with a built in admin UI served along side the database.

For the prototype, this always runs with `rad serve`. When Rad matures a bit more, it will be removed, reworked and secured properly.
