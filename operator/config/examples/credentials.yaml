apiVersion: v1
kind: Secret
metadata:
  name: example-rad-s3
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: replace-me
  AWS_SECRET_ACCESS_KEY: replace-me
  # AWS_SESSION_TOKEN may be supplied for temporary STS credentials.
