apiVersion: v1
kind: Service
metadata:
  name: rad-operator-metrics
  labels:
    app.kubernetes.io/name: rad-operator
    app.kubernetes.io/part-of: rad
spec:
  selector:
    app.kubernetes.io/name: rad-operator
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
