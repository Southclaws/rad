The generative engine tests are buried deep, but they are probably the most important test tool in the entire codebase. I'd like these hoisted into ./tests to live as a first class sibling with the other top level testing tools.

Needs thoughts and design around private imports and dependencies.
