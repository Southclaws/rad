# e2e/ord_nulls_last_desc — nullable int64 column ordered descending: NULLs last.
tables:
  - id: 1
    name: nums
    columns:
      - { id: 1, name: id,  type: string, pk: true }
      - { id: 2, name: val, type: int64, nullable: true }
