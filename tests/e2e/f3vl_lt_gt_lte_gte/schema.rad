# e2e/f3vl_lt_gt_lte_gte — the four range comparisons on int64 and on text
# (lexical order), asserting the exact ordered kept set for each.
tables:
  - id: 1
    name: t
    columns:
      - { id: 1, name: id, type: string, pk: true }
      - { id: 2, name: n,  type: int64 }
      - { id: 3, name: s,  type: string }
