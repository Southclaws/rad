# e2e/agg_min_max — global min/max over an int64 column and over a text column.
tables:
  - name: readings
    columns:
      - { name: id,    type: string, pk: true }
      - { name: n,     type: int64 }
      - { name: label, type: string }
