# e2e/ord_text_lexical — ordering a text column lexically (by codepoint), not
# numerically: "1" < "10" < "2".
tables:
  - id: 1
    name: words
    columns:
      - { id: 1, name: id,   type: string, pk: true }
      - { id: 2, name: name, type: string }
