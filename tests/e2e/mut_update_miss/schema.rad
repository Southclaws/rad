# e2e/mut_update_miss — a negative fixture: updating a primary key that does
# not exist must fail the whole program with mutation_target_not_found.
tables:
  - name: items
    columns:
      - { name: id,   type: string, pk: true }
      - { name: name, type: string }
