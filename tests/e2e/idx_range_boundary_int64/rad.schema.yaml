# probe_idx_boundary — boundary values on an int64 index, including negatives
# (sign-flip encoding), exact endpoints, and one-past-the-end empty ranges.
tables:
  - id: 1
    name: t
    columns:
      - { id: 1, name: id, type: string, pk: true }
      - { id: 2, name: n,  type: int64 }
    indexes:
      - { columns: [n] }
