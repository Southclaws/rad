# e2e/errbind_unknown_table — a scan of a table absent from the catalog must
# be rejected by the binder as invalid with the reason unknown_table.
tables:
  - name: widgets
    columns:
      - { name: id,   type: string, pk: true }
      - { name: name, type: string }
      - { name: qty,  type: int64 }
