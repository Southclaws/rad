# e2e/errbind_shared_node — one node consumed by two different consumers
# violates the single-consumer rule. Code captured by running.
tables:
  - id: 1
    name: widgets
    columns:
      - { id: 1, name: id,   type: string, pk: true }
      - { id: 2, name: name, type: string }
      - { id: 3, name: qty,  type: int64 }
