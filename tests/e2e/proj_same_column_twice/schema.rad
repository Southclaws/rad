# probe: cross product and non-equi joins over two small tables.
tables:
  - name: lefts
    columns:
      - { name: lid, type: string, pk: true }
      - { name: a,   type: int64 }

  - name: rights
    columns:
      - { name: rid, type: string, pk: true }
      - { name: b,   type: int64 }
