# e2e/agg_empty_set — a global fold over a table filtered to ZERO rows still
# produces exactly one output row: count = 0, and sum/avg/min/max = NULL.
tables:
  - name: ledger
    columns:
      - { name: id,     type: string, pk: true }
      - { name: amount, type: int64 }
