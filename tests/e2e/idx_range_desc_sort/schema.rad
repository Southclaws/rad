# probe_idx_desc — desc ordering over an indexed column must force a sort
# (KV is ascending-only), so a desc + limit result must still be correct.
tables:
  - name: t
    columns:
      - { name: id, type: string, pk: true }
      - { name: n,  type: int64 }
    indexes:
      - { columns: [n] }
