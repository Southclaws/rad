# e2e/error_unknown_table — a negative fixture: a query against a table that
# does not exist must be rejected by the binder as an invalid request, with
# the fine-grained reason unknown_table.
tables:
  - name: items
    columns:
      - { name: id,   type: string, pk: true }
      - { name: name, type: string }
