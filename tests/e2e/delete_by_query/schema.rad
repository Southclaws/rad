# e2e/delete_by_query — a query-driven delete: the delete statement's input
# is a scan/filter/project selecting which rows to remove by primary key.
tables:
  - name: items
    columns:
      - { name: id,   type: string, pk: true }
      - { name: keep, type: bool }
