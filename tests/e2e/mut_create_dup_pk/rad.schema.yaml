# e2e/mut_create_dup_pk — a negative fixture: a single create statement whose
# `rows` relation carries two rows sharing a primary key must fail.
tables:
  - id: 1
    name: notes
    columns:
      - { id: 1, name: id,   type: string, pk: true }
      - { id: 2, name: body, type: string }
